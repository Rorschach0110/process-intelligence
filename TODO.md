# TODO

本文件用于跟踪 Process Intelligence 的开发任务和优先级。

## P0 - 架构治理

- [x] 将 `backend/server.py` 拆分为职责清晰的小模块，确保单文件不超过 200 行。
- [x] 将 `web/styles.css` 拆分为更小的样式文件，确保单文件不超过 200 行。
- [x] 为 pipeline 核心服务添加最小测试结构。
- [x] 确认短期后端技术路线：继续标准库 MVP；V0.2 完成后再评估 FastAPI + uv。
- [x] 确认短期存储路线：继续使用 CSV/JSON 文件；真实数据闭环完成后再评估 SQLite 或 DuckDB。

## P1 - V0.2 真实数据闭环

- [x] 增加 CSV 上传能力。
- [x] 将上传文件保存到 `data/uploads`。
- [x] 上传后返回字段列表和前 20 行数据预览。
- [x] 增加字段映射：`case_id`、`activity`、`timestamp`、`resource`、`energy_kwh`、`material_kg`、`device`。
- [x] 允许 `/api/run-pipeline` 针对用户选择的上传文件运行。
- [x] 将碳排因子从后端常量改为请求参数。
- [x] 校验缺失字段和异常字段格式。
- [x] 将每次 pipeline 运行结果保存为 JSON。

## P2 - 分析质量提升

- [x] 增加考虑活动持续时间的瓶颈识别。
- [x] 增加资源负载和利用率指标。
- [x] 增加更通用的返工循环识别，不依赖固定的 `Rework` 活动名。
- [x] 增加可配置的碳排因子配置方案。
- [x] 为每条优化建议增加置信度和证据字段。

## P3 - 知识图谱

- [x] 定义标准知识图谱 schema 文档。
- [x] 增加图谱 JSON 导出。
- [x] 增加 Cypher 语句导出。
- [x] 将 Neo4j 评估为可选适配器，而不是核心强依赖。
- [x] 增加按活动、资源、碳排指标查询图谱的接口。

## P4 - AI 优化

- [x] 定义 `OptimizerProvider` 接口。
- [x] 保留 `RuleBasedOptimizer` 作为离线兜底方案。
- [x] 增加 OpenAI 兼容接口的 provider 抽象。
- [x] 增加流程-碳排-图谱优化提示词模板。
- [x] 要求 LLM provider 返回结构化 JSON。
- [x] 增加 LLM 响应校验和兜底策略。

## P5 - 前端产品化

- [x] 增加上传和字段映射界面。
- [x] 增加可编辑碳排因子控件。
- [x] 增加 pipeline 运行历史。
- [x] 使用 Cytoscape.js 增强图谱可视化。
- [x] 增加报告导出视图。
- [x] 评估迁移到 Next.js + React + TypeScript + Tailwind CSS。

## P6 - 文档

- [x] 架构调整后同步更新 `docs/development-guide.md`。
- [x] 接口稳定后增加 API 参考文档。
- [x] 增加真实用户数据格式指南。
- [x] 增加部署指南。
- [x] 增加故障排查指南。

## P7 - 架构升级基础

- [x] 将成熟工业系统规划写入 `docs/system-roadmap.md`。
- [x] 增加 FastAPI 服务入口 `backend/app/main.py`。
- [x] 增加 `pyproject.toml` 和 uv 依赖管理。
- [x] 增加 SQLite 数据库 `data/process_intelligence.sqlite3`。
- [x] 建立数据集和 pipeline 运行摘要 repository。
- [x] 让上传文件登记为数据集。
- [x] 让 pipeline 运行历史同步写入 SQLite。
- [x] 保留标准库入口作为兼容路径。
- [x] 增加 P7 repository 测试。

## P8 - 数据中心

- [x] 将上传文件、示例文件统一建模为 Dataset。
- [x] 增加 `GET /api/datasets/{id}` 数据集详情接口。
- [x] 增加数据预览分页接口，避免一次返回过多行。
- [x] 增加字段映射模板表和 repository。
- [x] 增加保存、读取、应用字段映射模板的 API。
- [x] 增加数据质量报告：缺失字段、空值比例、时间解析错误、数值异常。
- [x] 将上传历史、校验结果和映射规则写入 SQLite。
- [x] 提供数据集列表、数据集详情和映射模板接口，供前端产品化接入。
- [x] 为数据集 repository、数据质量检查和 API 增加测试。

## P9 - 流程挖掘中心

- [x] 固化流程分析结果 schema，便于前后端和报告复用。
- [x] 拆分活动执行时间、等待时间和案例总时长。
- [x] 增加流程变体详情接口和变体占比统计。
- [x] 增加流程对比能力：数据集 A vs 数据集 B、时间段 A vs 时间段 B。
- [x] 增加返工路径详情，不只输出重复活动摘要。
- [x] 增加合规偏差识别：必经活动缺失、异常顺序、跳步。
- [x] 增加流程健康评分，综合瓶颈、返工、变体复杂度和数据质量。
- [x] 提供流程中心接口：变体、瓶颈、返工、健康评分，供前端产品化接入。
- [x] 为流程挖掘核心算法增加单元测试和快照测试。

## P10 - 碳排核算中心

- [x] 建立碳因子库表：因子名称、类型、单位、值、来源、版本、启用状态。
- [x] 支持电力、物料、设备、工序、区域电网和时间段因子。
- [x] 增加因子版本管理和历史追溯。
- [x] 将 pipeline 运行绑定到使用的碳因子版本。
- [x] 增加 Scope 1、Scope 2、Scope 3 分类字段。
- [x] 增加活动、资源、订单、产品维度碳排聚合。
- [x] 增加单位产量碳强度和单案例碳强度。
- [x] 提供碳因子库管理和碳排明细接口，供前端产品化接入。
- [x] 增加碳核算方法说明文档和测试。

## P11 - 知识图谱中心

- [x] 扩展图谱 schema：Case、Device、Material、Product、Bottleneck、Recommendation。
- [x] 增加图谱 schema 版本号和兼容策略。
- [x] 将图谱节点与数据集、运行任务关联。
- [x] 增加图谱查询 API：按节点类型、关系类型、碳排区间、资源名称过滤。
- [x] 增加图谱子图接口，支持围绕活动、资源或指标展开。
- [x] 增加 Neo4j 可选 adapter，并保持 JSON schema 为核心。
- [x] 增加 Cypher 导入脚本和导出测试。
- [x] 提供图谱搜索、过滤、聚焦、邻居展开和详情接口，供前端产品化接入。
- [x] 更新 `docs/knowledge-graph-schema.md`。

## P12 - 优化中心

- [x] 增强 `RuleBasedOptimizer`，按碳排、瓶颈、返工、资源负载分别生成建议。
- [x] 增加优化建议表，保存建议版本、状态、审核人和采纳结果。
- [x] 增加 LLM provider 配置接口，支持 base_url、model、启用状态。
- [x] 增加 LLM 调用审计：提示词摘要、响应校验、失败原因、兜底记录。
- [x] 增加建议风险字段：产能风险、质量风险、实施难度、验证方式。
- [x] 增加优化建议审核流：待审核、已采纳、已拒绝、已实施。
- [x] 提供优化中心和建议审核接口，供前端产品化接入。
- [x] 为 provider 校验、兜底策略和建议持久化增加测试。

## P13 - 仿真与方案比较

- [x] 定义 What-if 场景 schema：调整活动、资源、碳因子、返工率和产能参数。
- [x] 增加场景表和方案表，保存输入参数和输出结果。
- [x] 增加单方案评估：预计减排、产能影响、成本影响和风险评分。
- [x] 增加多方案对比和优先级排序。
- [x] 将优化建议一键转为仿真场景。
- [x] 增加基线 vs 场景的图表数据接口。
- [x] 提供仿真中心接口：场景配置、结果对比、方案排序，供前端产品化接入。
- [x] 为仿真计算和方案排序增加测试。

## P14 - 报告中心

- [x] 定义报告模板 schema：管理层摘要、工程明细、碳管理、设备维护。
- [x] 增加报告生成服务，汇总数据质量、流程、碳排、图谱和优化建议。
- [x] 增加 HTML 报告导出。
- [x] 增加 PDF 导出方案评估和最小实现。
- [x] 增加报告表，保存报告类型、关联运行、生成时间和文件路径。
- [x] 增加报告审计说明：数据来源、映射规则、碳因子版本、算法版本。
- [x] 提供报告中心接口：模板选择、预览、导出和历史，供前端产品化接入。
- [x] 为报告生成和导出增加测试。

## P15 - 工业化部署

- [x] 增加用户、角色、组织、工厂、车间、产线基础模型。
- [x] 增加最小角色权限：Admin、Engineer、Viewer。
- [x] 增加操作审计日志：上传、运行、修改因子、审核建议、导出报告。
- [x] 增加配置中心：LLM、存储、图谱、碳因子默认值。
- [x] 增加健康检查增强：数据库、上传目录、运行目录、依赖版本。
- [x] 增加诊断包导出：配置摘要、最近日志、数据库统计、环境信息。
- [x] 增加备份恢复脚本：SQLite、uploads、runs、reports。
- [x] 增加 Dockerfile 和部署说明。
- [x] 增加生产验收 checklist 和端到端冒烟测试。

## P16 - 模块化前端产品界面

- [x] 新建 `frontend/`，保留 `web/` 作为 legacy 工作台。
- [x] 使用 Next.js App Router、React、TypeScript Strict、Tailwind CSS 4。
- [x] 增加左侧导航、顶部工作区栏和产品级 App Shell。
- [x] 增加统一 API client、Zustand 工作区 store、格式化工具和类型定义。
- [x] 增加 Dashboard 总览页。
- [x] 增加数据中心页面。
- [x] 增加流程挖掘中心页面。
- [x] 增加碳排核算中心页面。
- [x] 增加 Cytoscape.js 知识图谱页面。
- [x] 增加优化决策中心页面。
- [x] 增加仿真评估中心页面。
- [x] 增加报告中心页面。
- [x] 增加系统运维页面。
- [x] 配置 Next.js API rewrite 到 FastAPI 后端。
- [x] 让服务端组件直连 FastAPI，避免 SSR 页面经 Next rewrite 自身绕路导致开发服务超时。
- [x] 补充 `frontend/README.md`，说明模块边界、目录结构、后端连接和验证方式。
- [x] 访问 `/`、`/data`、`/graph`、`/ops` 和 `/api/health` 完成运行时冒烟验证。
- [x] 通过 `pnpm run lint` 和 `pnpm run build`。

## P17 - 前端工作台骨架升级

- [x] 参考 `chat-process` 多应用工作台，把当前导航升级为分组导航：数据与知识、流程与碳、优化与仿真、资产与运维。
- [x] 顶部工作区栏增加当前数据集、当前运行任务、后端健康状态和快捷运行入口。
- [x] 新增通用 `Toolbar`、`SearchBox`、`EmptyState`、`DataTable`、`DrawerShell`、`StepShell` 组件。
- [x] 为所有模块补齐加载态、空状态、错误态和禁用态。
- [x] 将当前静态中心页拆成业务目录，避免页面文件继续膨胀。
- [x] 保持 `web/` 为 legacy 对照工作台，不迁移或删除。

## P18 - 数据与文件知识库

- [x] 参考 `chat-process` 的 `EnergyKB`，将 `/data` 升级为知识库式数据中心。
- [x] 增加左侧分类栏：全部、事件日志、量化结果、报告、因子来源、设备资料。
- [x] 增加搜索、类型过滤、刷新、上传文件工具栏。
- [x] 增加数据集卡片网格和表格视图切换。
- [x] 增加上传 modal、编辑元数据 modal、删除确认 modal。
- [x] 增加数据集详情页：字段概览、分页预览、数据质量、字段映射模板。
- [x] 支持将数据集设为当前工作区数据源。
- [x] 对接 `GET /api/datasets`、`GET /api/datasets/{id}`、`GET /api/datasets/{id}/preview`、`POST /api/upload-csv`。

## P19 - 量化建模向导

- [x] 新增 `/quantification` 路由，参考 `chat-process` 的 `EnergyMultiScale` 步骤式页面。
- [x] 步骤一：选择知识库数据集或上传新 CSV。
- [x] 步骤二：字段映射，支持自动建议、手动修改、必填字段检查和冲突检查。
- [x] 步骤三：选择碳因子版本并支持临时覆盖因子。
- [x] 步骤四：执行 pipeline，展示进度、错误、结果摘要和量化结果预览。
- [x] 支持保存运行结果、同步到知识库、进入流程地图工作区。
- [x] 建立 `useQuantificationStore` 管理向导步骤和运行上下文。

## P20 - 流程地图与图谱联动

- [x] 新增 `/process-map` 路由，参考 `chat-process` 的 ProcessMap 和 GCE_DFG 交互。
- [x] 增加流程图工具栏：运行记录、活动阈值、路径阈值、指标切换和导出。
- [x] 流程节点支持点击、右键菜单、高亮上下游、双击重置。
- [x] 新增活动详情抽屉：时长、碳排、资源负载、返工、建议。
- [x] 新增 `/graph-workbench` 或增强 `/graph`，参考 LCE_KG 四层追溯图。
- [x] 图谱支持查询、过滤、邻居展开、聚焦、时间滑块和节点详情抽屉。
- [x] 实现流程图活动选择后联动图谱追溯。

## P21 - 碳因子库与根因库

- [x] 参考 `chat-process` 的 `FactorLibrary`，将 `/carbon` 升级为可管理的碳因子库。
- [x] 增加因子统计卡、搜索、表格、多选、添加、编辑、删除、导出。
- [x] 支持因子类型、Scope、版本、来源、启用状态和适用范围展示。
- [x] 新增根因库页面，参考 `RootCauseLibrary`。
- [x] 根因库支持原因、结果、置信度、证据、来源数据集和关联建议。
- [x] 支持从图谱路径和优化建议沉淀根因记录。

## P22 - 优化审核与仿真向导

- [x] 将 `/optimization` 升级为优化建议审核台。
- [x] 建议卡片展示综合评分、预计减排、风险等级、实施难度和状态。
- [x] 建议详情展示证据、影响、风险、验证方式和关联图谱路径。
- [x] 支持采纳、拒绝、转仿真、生成报告动作。
- [x] 将 `/simulation` 升级为五步方案向导：选择基线、配置参数、生成方案、方案对比、仿真导出。
- [x] 参考 `chat-process` 调度模块增加方案卡片、对比表、甘特图或时间线、导出 modal。

## P23 - 报告与资产中心

- [x] 报告中心增加报告列表、搜索、刷新、新建报告、预览和导出。
- [x] 报告详情页增加目录、HTML 预览、审计信息和导出入口。
- [x] 新增资产中心，参考 `chat-process` 的 `Asset3D` 列表模式。
- [x] 资产中心短期支持工厂、车间、产线、设备台账和分类筛选。
- [x] 资产详情展示关联活动、碳排、运行任务、图谱节点和优化建议。
- [x] 中期预留 GLB/GLTF 上传、模型预览、节点聚焦和隔离接口。

## P24 - 前端质量闭环

- [x] 增加前端测试框架，优先覆盖 store、API client 和关键组件。
- [x] 增加 Playwright 冒烟测试：数据上传、量化向导、流程地图、图谱、报告。
- [x] 增加页面截图检查，防止功能退化为静态贴图。
- [x] 增加前端构建体积和重型图表动态加载检查。
- [x] 所有阶段通过 `pnpm run lint`、`pnpm run build`、后端 `uv run pytest`。
